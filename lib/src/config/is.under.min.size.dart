import 'package:flutter/material.dart';

bool isUnderMinSize(Size size, [Size minimumSize = const Size(1170, 550)]) =>
    // size.height < minimumSize.height || size.width < minimumSize.width;
    false;
